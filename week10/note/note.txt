system函数是先阻塞自己，然后创建一个新的pid，等这个新建的pid结束后，唤醒systemexec函数簇是用新的代码替换当前的代码

return值：
0：成功
1-125：未成功，由命令定义
126：命令存在，但无法运行
127：命令不存在
>128：命令同收到信号的

本次作业：在执行fork/vfork操作后，父进程会复制所有的资源，包括文件描述符给子进程。编程验证父子进程对于相同文件操作时，是否会相互影响，即两者是会共享文件指针，还是拥有自己各自独立的指针体系。请编写两个版本（fork版和vfork版），实验结果贴图于答案区并附文字说明。源代码附件形式提交。
